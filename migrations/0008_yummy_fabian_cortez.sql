CREATE TABLE `ui_kit_settings` (
	`key` text PRIMARY KEY NOT NULL,
	`screenshots_json` text NOT NULL,
	`preview_video_url` text NOT NULL,
	`features_video_url` text NOT NULL,
	`install_video_url` text NOT NULL
);
